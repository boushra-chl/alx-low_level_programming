#include <stdio.h>

/**
 * main - function that prints the program's name
 *
 * Return: 0 on Success
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

